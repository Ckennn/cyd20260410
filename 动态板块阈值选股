# 🚀 动态选股策略优化行动指南 (Action Plan)

## 1. 核心思想转变
*   **从“抓反转”到“抓确认”**：放弃预测第一波龙头启动，利用 **T+4 滞后入场** 机制，确认板块效应（Trend Confirmation）形成后再介入，吃第二/三波主升浪。
*   **从“绝对值”到“相对势”**：买入不仅看热度是否达标，更看热度是否 **增长**；卖出看热度是否 **高位回落** 或 **低位熔断**。
*   **从“固定参数”到“动态矩阵”**：不同行业（工程建设 vs 电池）拥有独立的买卖阈值，且未来支持通过机器学习动态调整。

## 2. 策略逻辑细化 (Strategy Logic)

### A. 买入规则 (Buy Rules)
> **目标**：确认板块效应扩散，右侧交易。

1.  **触发条件**：
    *   **数量达标**：过去 3 天内，至少有 2 天的 `sig_count`（触发信号个股数） >= `Buy_Threshold`。
    *   **趋势增长**：确认个股数量在增加（例如 `count(T) >= count(T-1)` 或 `count(T) >= count(T-2)`）。
2.  **执行动作**：
    *   **延迟入场**：满足条件后，在 **T+4 开盘** 买入该板块内触发信号的个股。
    *   **板块竞争**：若同一天多个板块满足条件，优先选择 `sig_count` 更大的板块（强者恒强）。

### B. 卖出规则 (Sell Rules)
> **目标**：高位止盈（人声鼎沸时离场），低位止损（防止阴跌）。

1.  **高位回落 (High-Level Cooldown)**：
    *   **触发**：当 `sig_count` 曾超过 `Sell_Threshold_High`（高位阈值），随后出现连续 2 天下降（T-1 < T-2 且 T < T-1）。
    *   **执行**：在 **T+4 开盘** 卖出该板块所有持仓。
2.  **低位止损 (Low-Level Stop)**：
    *   **触发**：当 `sig_count` 直接低于 `Sell_Threshold_Low`（低位阈值，如 10）。
    *   **执行**：在 **T+2 开盘** 迅速卖出（防止板块彻底转冷）。

## 3. 行业参数矩阵 (Initial Parameters)
我们将首先在代码中硬编码这组参数，作为基准测试。后续将通过机器学习模型动态更新此表。

| 序号 | 板块名称 | 总股数 | 买入阈值 (Buy) | 高位卖出 (Sell_High) | 低位卖出 (Sell_Low) | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **工程建设** | 69 | 11 | 23 | 10 | 稳健型 |
| 2 | **家电行业** | 92 | 15 | 35 | 10 | 消费类，阈值较高 |
| 3 | **珠宝首饰** | 17 | N/A | N/A | N/A | 股数太少，可能需忽略 |
| 4 | **农牧饲鱼** | 87 | 15 | 30 | 10 | 周期性强 |
| 5 | **电池** | 75 | 15 | 23 | 10 | 成长型，波动大 |
| 6 | **贸易行业** | 36 | 12 | 29 | 10 | 股数少但阈值高 |
| ... | **默认** | - | 15% (占比) | 30% (占比) | 5% (占比) | 这里的百分比需换算为具体股数 |

## 4. 实施步骤 (Implementation Steps)

### Phase 1: 策略逻辑改造 (Code Refactoring)
*   **目标**：修改 `industryanalysis.py`，实现上述 T+4 买入与 T+2/T+4 卖出逻辑。
*   **动作**：
    1.  更新 `industry_parameters_model2.csv` 或在代码中通过字典配置上述参数。
    2.  重写 `get_continue_n_result` 函数，加入“趋势增长”判断和“板块竞争”逻辑。
    3.  调整买卖信号生成的 `trade_date`（T+4 / T+2）。

### Phase 2: 动态阈值接口预留 (Interface Design)
*   **目标**：让策略能读取外部生成的阈值。
*   **动作**：
    1.  设计 `DynamicThresholdLoader` 类，优先读取 `industry_dynamic_thresholds.csv`。
    2.  如果文件不存在，则回退到默认的静态参数。

### Phase 3: 回测与验证 (Backtesting)
*   **目标**：验证新逻辑是否降低了回撤。
*   **动作**：
    1.  运行 Step 3 生成新的指令。
    2.  运行 Step 5 (行业ETF回测) 快速验证宏观效果。
    3.  检查是否做到了“回撤 < 10%”。

---
